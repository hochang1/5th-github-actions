name: Welcome Team Member
run-name: "새 팀원 환영 - ${{ github.actor }}"   # 추가
on:
  workflow_dispatch

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: 인사 출력
        run: echo "Hello, GitHub Actions!"

      - name: 정보
        run: |
          echo "오늘은 당신의 첫 출근일입니다"
          echo "프로젝트: GitHub Actions 학습"
          echo "팀: DevOps"
          echo "Runner OS: Linux"

      - name: 끝인사 
        run: echo "=== 행운을 빕니다! ==="
